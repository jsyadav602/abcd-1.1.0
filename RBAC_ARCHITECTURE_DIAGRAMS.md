# 🏗️ RBAC System Architecture Diagrams

## 1. Overall System Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                          USER LOGIN                                  │
│                      (Frontend - index.html)                         │
└────────────────────────┬─────────────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │  POST /api/auth/login              │
        │  {loginId, password, deviceId}    │
        └────────────┬───────────────────────┘
                     │
                     ▼
        ╔════════════════════════════════════╗
        ║      BACKEND - AUTH SERVICE        ║
        ║  1. Verify password                ║
        ║  2. Fetch user + atomicRole        ║
        ║  3. Extract permissions from role  ║
        ║  4. Generate JWT token            ║
        ╚════════════┬═══════════════════════╝
                     │
                     ▼
        ┌────────────────────────────────────┐
        │  LOGIN RESPONSE:                   │
        │  {                                 │
        │    user: {...},                   │
        │    permissions: [                 │
        │      "user:create",               │
        │      "asset:read",                │
        │      "report:view"                │
        │    ],                             │
        │    accessToken: "jwt...",         │
        │    refreshToken: "refresh..."     │
        │  }                                 │
        └────────────┬───────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────┐
        │ FRONTEND - Store in localStorage   │
        │ authData = {user, permissions,    │
        │            accessToken}           │
        └────────────┬───────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────┐
        │ FRONTEND - Permission UI Control   │
        │ • Hide elements without permission │
        │ • Disable buttons                  │
        │ • Show admin sections              │
        └────────────┬───────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────┐
        │  USER CLICKS ACTION BUTTON         │
        │  (e.g., "Delete User")             │
        └────────────┬───────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────┐
        │ FRONTEND - Check Permission        │
        │ hasPermission("user:delete") ?     │
        │ • true → Send API request          │
        │ • false → Show error               │
        └────────────┬───────────────────────┘
                     │ (with JWT token)
                     ▼
        ╔════════════════════════════════════╗
        ║   BACKEND - ROUTE HANDLER          ║
        ║   DELETE /api/users/:id            ║
        ╚════════════════════════════════════╝
                     │
                     ▼
        ┌─────────────────────────────────────────────┐
        │ MIDDLEWARE CHAIN:                           │
        │                                             │
        │  ┌────────────────────────────────────────┐ │
        │  │ 1. verifyJWT Middleware                │ │
        │  │    • Extract user from token           │ │
        │  │    • Verify token signature            │ │
        │  │    • Return 401 if invalid             │ │
        │  └────────────┬─────────────────────────┘ │
        │               │                            │
        │               ▼                            │
        │  ┌────────────────────────────────────────┐ │
        │  │ 2. populatePermissions Middleware     │ │
        │  │    • Fetch user's atomicRole          │ │
        │  │    • Extract permissions from role    │ │
        │  │    • Attach to req.user.permissions   │ │
        │  └────────────┬─────────────────────────┘ │
        │               │                            │
        │               ▼                            │
        │  ┌────────────────────────────────────────┐ │
        │  │ 3. checkPermission Middleware         │ │
        │  │    • Check: user has "user:delete"?  │ │
        │  │    • wildcard "*" → pass              │ │
        │  │    • exact match → pass                │ │
        │  │    • no match → return 403             │ │
        │  └────────────┬─────────────────────────┘ │
        │               │                            │
        │               ▼                            │
        │  ┌────────────────────────────────────────┐ │
        │  │ 4. Route Handler                       │ │
        │  │    • Verify scope access               │ │
        │  │    • Execute action                    │ │
        │  │    • Return result                     │ │
        │  └────────────────────────────────────────┘ │
        └─────────────────────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────┐
        │  API RESPONSE                      │
        │  • 200 OK + data (success)         │
        │  • 403 Forbidden (permission)      │
        │  • 401 Unauthorized (auth)         │
        └────────────┬───────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────┐
        │  FRONTEND - Handle Response        │
        │  • Success → Update UI              │
        │  • Error → Show message             │
        └────────────────────────────────────┘
```

---

## 2. Database Schema Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                       PERMISSION                            │
├─────────────────────────────────────────────────────────────┤
│ _id: ObjectId                                               │
│ key: "user:create" (UNIQUE)                                 │
│ description: "Create new user accounts"                     │
│ category: "user_management"                                 │
│ isActive: true                                              │
│ createdAt: Date                                             │
└─────────────────────────────────────────────────────────────┘
                           ▲
                           │ referenced in
                           │ permissions[]
                           │
┌─────────────────────────────────────────────────────────────┐
│                       ATOMIC ROLE                           │
├─────────────────────────────────────────────────────────────┤
│ _id: ObjectId                                               │
│ name: "branch_admin" (UNIQUE)                               │
│ displayName: "Branch Administrator"                         │
│ category: "system"                                          │
│ permissions: [                                              │
│   "user:create",   ─────────────┐                          │
│   "user:update",                 ├─→ Permission refs      │
│   "asset:delete"   ──────────────┘                         │
│ ]                                                           │
│ organizationId: null                                        │
│ priority: 3                                                 │
│ isProtected: true                                           │
│ createdAt: Date                                             │
└─────────────────────────────────────────────────────────────┘
                           ▲
                           │ assigned via
                           │ atomicRoleId
                           │
┌─────────────────────────────────────────────────────────────┐
│                          USER                               │
├─────────────────────────────────────────────────────────────┤
│ _id: ObjectId                                               │
│ userId: "USR001"                                            │
│ name: "John Doe"                                            │
│ email: "john@company.com"                                   │
│ atomicRoleId: ObjectId ────────→ AtomicRole                │
│ permissions: [...]  (denormalized from role)                │
│ organizationId: ObjectId ─────→ Organization                │
│ branchId: [ObjectId, ...] ───→ Branch(es)                   │
│ canLogin: true                                              │
│ isActive: true                                              │
│ isBlocked: false                                            │
│ createdAt: Date                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. Permission Check Flow (Detailed)

```
REQUEST ARRIVES
      │
      ▼
┌─────────────────────────────────────────┐
│ 1. EXTRACT JWT TOKEN                    │
│    From Authorization header            │
│    "Bearer eyJhbGc..."                  │
└────────────────┬────────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Token Valid?       │
        └────┬────────┬──────┘
             │ YES    │ NO
             │        └─────→ Return 401 Unauthorized
             │
             ▼
┌─────────────────────────────────────────┐
│ 2. VERIFY USER STILL EXISTS              │
│    Check User collection                │
│    canLogin? isActive? isBlocked?       │
└────────────────┬────────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ User Valid?        │
        └────┬────────┬──────┘
             │ YES    │ NO
             │        └─────→ Return 403 Forbidden / 401
             │
             ▼
┌─────────────────────────────────────────┐
│ 3. POPULATE PERMISSIONS                 │
│    • Get atomicRoleId from user         │
│    • Fetch AtomicRole from database     │
│    • Extract permissions array          │
│    • Attach to req.user.permissions     │
└────────────────┬────────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Has Wildcard "*"?  │
        └────┬────────┬──────┘
             │ YES    │ NO
             │        │
             │        ▼
             │    ┌──────────────────────┐
             │    │ Exact Permission     │
             │    │ Match?               │
             │    └────┬────────┬────────┘
             │         │ YES    │ NO
             │         │        │
             └─────────┤        └─────→ Return 403 Forbidden
                       │
                       ▼
       ┌──────────────────────────────┐
       │ 4. VERIFY SCOPE ACCESS       │
       │    • Check branch access      │
       │    • Check enterprise access  │
       │    • Verify user can access   │
       │      this branch/enterprise   │
       └────────────┬─────────────────┘
                    │
                    ▼
           ┌────────────────────┐
           │ Scope Access OK?   │
           └────┬────────┬──────┘
                │ YES    │ NO
                │        └─────→ Return 403 Access Denied
                │
                ▼
       ┌──────────────────────────────┐
       │ 5. EXECUTE ROUTE HANDLER     │
       │    User has:                  │
       │    • Valid auth               │
       │    • Required permission      │
       │    • Scope access             │
       │    → Proceed with action      │
       └────────────┬─────────────────┘
                    │
                    ▼
           ┌────────────────────┐
           │ Success?           │
           └────┬────────┬──────┘
                │ YES    │ NO
                │        │
                ▼        ▼
       ┌──────────────────────────────┐
       │ 6. RETURN RESPONSE           │
       │    200 OK + Data             │
       │    OR Error (4xx/5xx)        │
       └──────────────────────────────┘
```

---

## 4. Frontend Permission Control Flow

```
┌──────────────────────────────────┐
│   PAGE LOADS                     │
│   DOMContentLoaded event fires   │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ initPermissionControls()         │
│ • Get permissions from storage   │
│ • Query all [data-permission]    │
│ • Query all buttons               │
│ • Query admin sections            │
└────────────┬─────────────────────┘
             │
             ▼
     ┌───────────────────┐
     │ For each element  │
     └────────┬──────────┘
              │
              ▼
    ┌─────────────────────────────────┐
    │ Extract data-permission value   │
    │ e.g., "user:create"              │
    │ or "user:create|user:update"    │
    │ or "user:create&user:delete"    │
    └────────┬────────────────────────┘
             │
             ▼
    ┌─────────────────────────────────┐
    │ Parse permission logic          │
    │ • "user:create"        → Single │
    │ • "x|y"                → OR     │
    │ • "x&y"                → AND    │
    └────────┬────────────────────────┘
             │
             ▼
    ┌─────────────────────────────────┐
    │ Check hasPermission(user, perm) │
    │ • Get current permissions       │
    │ • Check for "*" wildcard        │
    │ • Check exact match             │
    └────────┬────────────────────────┘
             │
             ▼
        ┌─────────────────┐
        │ Result?         │
        └────┬────────┬───┘
             │ YES    │ NO
             │        │
             ▼        ▼
      ┌──────────┐  ┌──────────────┐
      │ Show     │  │ Hide/Disable │
      │ Element  │  │ Element      │
      │ Enable   │  │              │
      │ Button   │  │              │
      └──────────┘  └──────────────┘
             │             │
             └──────┬──────┘
                    │
                    ▼
    ┌─────────────────────────────────┐
    │ USER INTERACTS                  │
    │ Clicks button, types in field   │
    └────────┬────────────────────────┘
             │
             ▼
    ┌─────────────────────────────────┐
    │ executeIfPermitted(perm, fn)    │
    │ Check permission again          │
    │ • Has permission?               │
    │   → Execute callback            │
    │ • No permission?                │
    │   → Show error message          │
    └─────────────────────────────────┘
```

---

## 5. Role Hierarchy

```
                    ┌──────────────────┐
                    │   SUPER_ADMIN    │
                    │   Priority: 1    │
                    │  Permissions: *  │
                    │  (All 37)        │
                    └────────┬─────────┘
                             │
                    Can manage everything
                    • All users
                    • All assets
                    • All reports
                    • All organizations
                    • All branches
                             │
                    ┌────────▼─────────┐
                    │ ENTERPRISE_ADMIN │
                    │ Priority: 2      │
                    │ Permissions: 15  │
                    └────────┬─────────┘
                             │
                    Can manage:
                    • Assigned enterprises
                    • Multiple branches
                    • Users in branches
                    • Assets in branches
                    │
    ┌───────────────┴──────────────────┬─────────────────┐
    │                                  │                 │
    ▼                                  ▼                 ▼
┌──────────────┐              ┌──────────────┐  ┌──────────┐
│ BRANCH_ADMIN │              │ BRANCH_ADMIN │  │   USER   │
│ Priority: 3  │              │ Priority: 3  │  │Priority: │
│Permissions:12│              │Permissions:12│  │   100    │
└──────────────┘              └──────────────┘  │Perm: 3   │
│                             │                 │          │
Can manage:                Can manage:          Can manage:│
• Branch 1                  • Branch 2          • Use own  │
• Users                     • Users             │ profile  │
• Assets                    • Assets            │ View     │
│                           │                   │ assets   │
└───────────┬───────────────┘                   │ View     │
            │                                    │ reports  │
   Scope constrained to                         └──────────┘
   assigned branches                           Limited scope
                                               (no creation)
```

---

## 6. Scope Access Matrix

```
User: John (BRANCH_ADMIN)
  ├─ organizationId: ORG_001
  └─ branchId: [BRANCH_1, BRANCH_2]

Resource: Asset
  ├─ organizationId: ORG_001
  └─ branchId: BRANCH_1

        ┌────────────────────────────────────┐
        │ SCOPE CHECK                        │
        ├────────────────────────────────────┤
        │                                    │
        │ Resource Branch = BRANCH_1         │
        │ User Branches = [BRANCH_1, BRANCH]│
        │ Match? YES → ✅ Access Allowed    │
        │                                    │
        ├────────────────────────────────────┤
        │                                    │
        │ Resource Organization = ORG_001   │
        │ User Organization = ORG_001       │
        │ Match? YES → ✅ Access Allowed    │
        │                                    │
        └────────────────────────────────────┘
                       │
                       ▼
            ✅ User CAN access this asset

---

Resource: Asset_2
  ├─ organizationId: ORG_001
  └─ branchId: BRANCH_3 (not assigned to user)

        ┌────────────────────────────────────┐
        │ SCOPE CHECK                        │
        ├────────────────────────────────────┤
        │                                    │
        │ Resource Branch = BRANCH_3         │
        │ User Branches = [BRANCH_1, BRANCH]│
        │ Match? NO → ❌ Access DENIED      │
        │                                    │
        └────────────────────────────────────┘
                       │
                       ▼
            ❌ User CANNOT access this asset
```

---

## 7. Permission Assignment Flow (Admin Task)

```
┌──────────────────────────────────────┐
│ ADMIN PORTAL: Manage Roles           │
└────────────────┬─────────────────────┘
                 │
                 ▼
    ┌─────────────────────────────────┐
    │ List all system permissions     │
    │ (37 total)                      │
    │                                 │
    │ ☐ user:create                   │
    │ ☐ user:update                   │
    │ ☐ user:delete                   │
    │ ☐ asset:create                  │
    │ ☐ asset:delete                  │
    │ ... (37 total)                  │
    └────────────────┬────────────────┘
                     │
                     ▼
    ┌──────────────────────────────────┐
    │ SELECT FOR NEW ROLE:             │
    │ "asset_manager"                  │
    │                                  │
    │ ☑ asset:create                   │
    │ ☑ asset:read                     │
    │ ☑ asset:update                   │
    │ ☑ asset:delete                   │
    │ ☑ asset:assign                   │
    │ ☑ asset:transfer                 │
    │ ☑ report:view                    │
    │ ☑ report:export                  │
    │ ☐ user:create                    │
    │ ☐ user:delete                    │
    │ ... (others unchecked)           │
    └────────────────┬────────────────┘
                     │
                     ▼
    ┌──────────────────────────────────┐
    │ CREATE ROLE                      │
    │ name: "asset_manager"            │
    │ permissions: [                   │
    │   "asset:create",                │
    │   "asset:read",                  │
    │   "asset:update",                │
    │   "asset:delete",                │
    │   "asset:assign",                │
    │   "asset:transfer",              │
    │   "report:view",                 │
    │   "report:export"                │
    │ ]                                │
    └────────────────┬────────────────┘
                     │
                     ▼
    ┌──────────────────────────────────┐
    │ ASSIGN TO USERS                  │
    │                                  │
    │ ☑ Employee 1                     │
    │ ☑ Employee 2                     │
    │ ☐ Employee 3                     │
    └────────────────┬────────────────┘
                     │
                     ▼
         USER UPDATES SAVED
         Permissions updated immediately
         on next API request
```

---

## 8. Complete Request/Response Cycle

```
REQUEST:
┌──────────────────────────────────────────────┐
│ POST /api/users                              │
│ Authorization: Bearer eyJhbGc...             │
│ Content-Type: application/json               │
│                                              │
│ {                                            │
│   "name": "Jane Smith",                     │
│   "email": "jane@company.com",              │
│   "branchId": "BRANCH_001",                 │
│   "roleId": "role_user"                     │
│ }                                            │
└──────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────┐
│ BACKEND PROCESSING                          │
│                                              │
│ verifyJWT → ✅                              │
│   Token valid, extract user                 │
│                                              │
│ populatePermissions → ✅                     │
│   User has atomicRole "branch_admin"       │
│   Permissions: ["user:create", ...]        │
│                                              │
│ checkPermission("user:create") → ✅        │
│   User has "user:create" permission        │
│                                              │
│ checkScopeAccess(...) → ✅                  │
│   User assigned to BRANCH_001              │
│                                              │
│ CreateUser logic → ✅                       │
│   Save to database                          │
│                                              │
└──────────────────────────────────────────────┘
                    │
                    ▼
RESPONSE (Success):
┌──────────────────────────────────────────────┐
│ HTTP 201 Created                             │
│                                              │
│ {                                            │
│   "success": true,                          │
│   "statusCode": 201,                        │
│   "data": {                                  │
│     "_id": "user_123",                      │
│     "userId": "USR012",                     │
│     "name": "Jane Smith",                   │
│     "email": "jane@company.com",            │
│     "atomicRoleId": "role_user",            │
│     "branchId": ["BRANCH_001"],             │
│     "canLogin": false,                      │
│     "isActive": true,                       │
│     "createdAt": "2026-02-19T..."           │
│   },                                         │
│   "message": "User created successfully"    │
│ }                                            │
└──────────────────────────────────────────────┘

---

ALTERNATIVE RESPONSE (No Permission):
┌──────────────────────────────────────────────┐
│ HTTP 403 Forbidden                           │
│                                              │
│ {                                            │
│   "success": false,                         │
│   "statusCode": 403,                        │
│   "message": "Permission 'user:create'     │
│               required",                    │
│   "error": {                                 │
│     "required": "user:create",              │
│     "userPermissions": ["asset:read"],      │
│     "requiredScope": "BRANCH_001",          │
│     "userScope": ["BRANCH_002"]             │
│   }                                          │
│ }                                            │
└──────────────────────────────────────────────┘
```

---

**These diagrams show the complete flow of the RBAC system from login through authorization to execution.**
